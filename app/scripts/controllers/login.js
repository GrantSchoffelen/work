'use strict';

/**
 * @ngdoc function
 * @name patientApp.controller:LoginCtrl
 * @description
 * # LoginCtrl
 * Controller of the patientApp
 */
angular.module('patientApp')
  .controller('LoginCtrl', function ($scope, ENV_DEFAULT) {
  	console.log(ENV_DEFAULT.apiUrl)

  
  });
